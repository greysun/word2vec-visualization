These special characters are deleted from "out.json" file:
- For English data:

\
"
'

(For example, delete all characters "\": replace all "\" by "")

- For Japanese data:
"　\"                (convert to "")
""（"                (convert to "")
